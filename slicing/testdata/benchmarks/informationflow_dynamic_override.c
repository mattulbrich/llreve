#include "slicing_marks.h"

//Simelar to examples Listing 9
int foo(int heigh, int low) {
	int x,y,z;
	if ( x == 3) {
		__assert_sliced(
		y = heigh + 0);
	}
	z = y + 1;
	if ( x != 3) {
		low = z ;
	}
	return low ;
}
